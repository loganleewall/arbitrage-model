"""
Scraper interfaces for sportsbook and stats data sources.
Each scraper exposes a `scrape_*` function returning a pandas DataFrame
with columns suited for downstream arbitrage or modeling tasks.
"""
